import { EnlightUserInputPipe } from './enlight-user-input.pipe';

describe('EnlightUserInputPipe', () => {
  it('create an instance', () => {
    const pipe = new EnlightUserInputPipe();
    expect(pipe).toBeTruthy();
  });
});
